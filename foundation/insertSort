function insertionSort(nums) {
  for (let i = 1; i < nums.length; i++) {
    let temp = nums[i];
    for (var j = i - 1; temp < nums[j] && j >= 0; j--) {
      nums[j + 1] = nums[j];
    }
    nums[j + 1] = temp;
  }
  return nums;
}

const nums = [10, 5, 3, 8, 2, 6, 4, 7, 9, 1];

insertionSort(nums);
